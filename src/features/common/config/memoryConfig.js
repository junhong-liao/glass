module.exports = {
  MAX_BACKGROUND_MESSAGES: 50,   // allow much deeper history
  BACKGROUND_SESSION_RATIO: 0.5, // half of the window may come from ended sessions
  MAX_TOTAL_MESSAGES: 100,       // overall sliding-window cap
};